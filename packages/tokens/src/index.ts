// @bygsmart/tokens — the single source of design tokens (AR-04).
// Real tokens land in P1 (E1, deliverable 1.1), generated from one source; nothing
// else in the codebase may define a colour or a spacing step.

export const PLACEHOLDER_TOKEN = 'bygsmart-tokens' as const;
