{
  "name": "@bygsmart/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Universal responsive primitives (React Native + React Native Web). Renders; knows nothing of domain (SRP). Every component tested on both renderers (layer 7).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@bygsmart/tokens": "workspace:*",
    "@bygsmart/core": "workspace:*"
  },
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run"
  }
}
