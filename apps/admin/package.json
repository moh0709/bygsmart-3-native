{
  "name": "@bygsmart/admin",
  "version": "0.0.0",
  "private": true,
  "description": "Back-office — a separate DOM Vite app (admin.bygsmart.com): platform admin, org/billing, SMTP, promo codes, tool access, 3D wizard, and the PWA marketplace surface.",
  "type": "module",
  "dependencies": {
    "@bygsmart/core": "workspace:*",
    "@bygsmart/api-client": "workspace:*",
    "@bygsmart/tokens": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.0.5",
    "typescript": "^5.7.2",
    "vite": "^8.2.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx --max-warnings=0",
    "test": "vitest run --passWithNoTests"
  },
  "//": "Deliberately NO react-native-web: dense admin tables are where RNW costs and delivers nothing. This is the DOM half of D-12."
}
