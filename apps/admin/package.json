{
  "name": "@bygsmart/admin",
  "version": "0.0.0",
  "private": true,
  "description": "Back-office — a separate DOM Vite app (admin.bygsmart.com): platform admin, org/billing, SMTP, promo codes, tool access, 3D wizard, and the PWA marketplace surface.",
  "type": "module",
  "dependencies": {
    "@bygsmart/core": "workspace:*",
    "@bygsmart/api-client": "workspace:*",
    "@bygsmart/tokens": "workspace:*"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run"
  },
  "//": "Vite + React + DOM added at the P5-D admin init step. Deliberately NO react-native-web: dense admin tables are where RNW costs and delivers nothing."
}
