// ─────────────────────────────────────────────────────────────────────────────
// modules/client-portal — public surface. The client portal has NO dedicated
// code yet: bygherre (CLIENT) access today is the CLIENT role's scoped views
// inside the project tabs. Dedicated read-only portal surfaces land here when
// they are built; the manifest exists so the module id is registry-resolved.
// ─────────────────────────────────────────────────────────────────────────────

export {};
