/**
 * ⚠️ This file is intentionally empty of theme values.
 *
 * Tailwind v4 (via @tailwindcss/postcss) does NOT auto-load this JS config —
 * it would only be read through an `@config` directive in CSS, which we do
 * not use. All design tokens live in ONE place:
 *
 *   src/index.css  →  @theme { ... }   (Design System 2.0)
 *
 * Dark mode is class-based via `@custom-variant dark` in src/index.css.
 * Content detection is automatic in v4.
 *
 * Do not add theme values here — they would silently do nothing.
 */
export default {};
