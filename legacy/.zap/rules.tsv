# OWASP ZAP baseline rules — TAB-separated: <ruleId>	<IGNORE|WARN|FAIL>	<comment>
# Unlisted rules default to WARN (reported, non-blocking). The scan is run
# report-only (fail_action: false) so it always completes and publishes findings.
10015	IGNORE	Incomplete or no cache-control (immutable hashed assets handled by nginx)
10096	IGNORE	Timestamp disclosure (build hashes / asset timestamps, not sensitive)
