## Deployment Notes
#
# This file is a reference only.
# See deploy/nginx-docker.conf  — for the Nginx config inside the Docker container
# See deploy/nginx-vps-proxy.conf — for the host Nginx SSL reverse proxy config
# See deploy/DEPLOY_GUIDE.md   — full deployment guide for vps2
